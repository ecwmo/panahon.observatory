---
import DefaultLayout from '@/layouts/Default.astro'

import IndexPage from '@/vpages/index.vue'

const mapboxToken = import.meta.env.MAPBOX_TOKEN
---

<DefaultLayout>
  <IndexPage mapbox-token={mapboxToken} client:visible />
</DefaultLayout>
