---
import DefaultLayout from '@/layouts/Default.astro'

import ReportPage from '@/vpages/report.vue'

const mode = Astro.url.searchParams.get('view') ?? ''
---

<DefaultLayout>
  <ReportPage view-mode={mode} client:visible />
</DefaultLayout>
